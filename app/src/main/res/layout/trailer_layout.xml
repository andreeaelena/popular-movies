<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="start|center_vertical"
    android:padding="@dimen/trailer_layout_padding"
    android:background="@drawable/section_selector">

    <ImageView
        android:id="@+id/trailer_thumbnail"
        android:layout_width="@dimen/trailer_thumbnail_width"
        android:layout_height="@dimen/trailer_thumbnail_height"
        android:scaleType="center"
        android:src="@drawable/ic_play_circle_white"
        android:background="@drawable/trailer_thumbnail_selector"/>

    <TextView
        android:id="@+id/trailer_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/trailer_name_margin_start"
        android:textColor="@color/white"
        android:textSize="@dimen/trailer_name_text_size"/>

</LinearLayout>